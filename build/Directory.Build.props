<Project>
  <Import Project="../Directory.Build.props" />

  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <!-- Keep the running NUKE host outside the product artifact tree cleaned by the build. -->
    <UseArtifactsOutput>false</UseArtifactsOutput>
  </PropertyGroup>
</Project>
