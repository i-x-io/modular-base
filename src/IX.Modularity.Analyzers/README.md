# IX.Modularity.Analyzers

`IX.Modularity.Analyzers` enforces XML documentation for public modularity contracts and services, and recommends records for simple data objects.

The package contains analyzer assets only. Add it to a C# project as an analyzer package; it has no runtime library dependency.
