# IXM1004: Public services require complete XML documentation

Public service types must provide a non-empty `summary` and documentation for any generic type parameters. A service type ends in `Service`, or is an interface derived from an `I*Service` interface.
