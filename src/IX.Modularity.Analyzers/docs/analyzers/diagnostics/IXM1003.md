# IXM1003: Interface members require complete XML documentation

Non-service interface members must document their summary, generic type parameters, parameters, return value, and property value where each applies. Indexer parameters use `param`; events require a summary.
